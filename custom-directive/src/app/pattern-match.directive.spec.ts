import { PatternMatchDirective } from './pattern-match.directive';

describe('PatternMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new PatternMatchDirective();
    expect(directive).toBeTruthy();
  });
});
